import { Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const Auth = () => {
  return (
    <SafeAreaView>
      <Text>Captain Auth</Text>
    </SafeAreaView>
  );
}

export default Auth